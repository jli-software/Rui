# Screenshots fürs README

`rui.png` — was das README einbindet. Bis es die Datei gibt, zeigt GitHub
dort nur den Alt-Text.

Sinnvoll für das Bild:

- dunkles Farbschema, ein PowerShell-Script offen
- Statusleiste sichtbar (Sprache, Encoding, Zeilenende, Vim-Modus)
- Fensterbreite rund 1200 px, damit die Schrift im README lesbar bleibt

Der Ordner liegt unter `assets/`, nicht unter `assets/logo/` — dort stehen
die Quell-SVGs fürs Icon, die `scripts/build-icons.ps1` verarbeitet.
